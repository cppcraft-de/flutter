diff --git a/modules/skparagraph/include/Metrics.h b/modules/skparagraph/include/Metrics.h
index 2c9821ae28..b254d7d74d 100644
--- a/modules/skparagraph/include/Metrics.h
+++ b/modules/skparagraph/include/Metrics.h
@@ -86,6 +86,24 @@ public:
     // Zero indexed line number
     size_t fLineNumber = 0;
 
+    // Diagnostic metrics from the final laid-out line's InternalLineMetrics.
+    double fRawAscent = 0.0;
+    double fRawDescent = 0.0;
+    double fRawLeading = 0.0;
+    double fEffectiveAscent = 0.0;
+    double fEffectiveDescent = 0.0;
+    double fEffectiveLeading = 0.0;
+    double fHeightInputAscent = 0.0;
+    double fHeightInputDescent = 0.0;
+    double fHeightInputLeading = 0.0;
+    double fHeightInputRawLeading = 0.0;
+    // 0: normal SkParagraph height, 2: Qt/FreeType size-metric baseline pitch.
+    double fLineHeightBranch = 0.0;
+    // The vertical advance used when positioning the next line.
+    double fNextLineBaselinePitch = 0.0;
+    // The occupied line height, tracked separately from baseline pitch.
+    double fLineBoxHeight = 0.0;
+
     // Mapping between text index ranges and the FontMetrics associated with
     // them. The first run will be keyed under start_index. The metrics here
     // are before layout and are the base values we calculate from.
diff --git a/modules/skparagraph/include/Paragraph.h b/modules/skparagraph/include/Paragraph.h
index a7112c8f39..8b494b4f58 100644
--- a/modules/skparagraph/include/Paragraph.h
+++ b/modules/skparagraph/include/Paragraph.h
@@ -2,6 +2,7 @@
 #ifndef Paragraph_DEFINED
 #define Paragraph_DEFINED
 
+#include "include/core/SkFontMetrics.h"
 #include "include/core/SkPath.h"
 #include "modules/skparagraph/include/FontCollection.h"
 #include "modules/skparagraph/include/Metrics.h"
@@ -112,6 +113,24 @@ public:
     using ExtendedVisitor = std::function<void(int lineNumber, const ExtendedVisitorInfo*)>;
     virtual void extendedVisit(const ExtendedVisitor&) = 0;
 
+    struct GlyphDiagnostic {
+        int fLineNumber;
+        size_t fRunIndex;
+        SkFont fFont;
+        SkFontMetrics fFontMetrics;
+        SkGlyphID fGlyph;
+        uint32_t fUtf8Cluster;
+        SkPoint fShapePosition;
+        SkPoint fOffset;
+        SkVector fAdvance;
+        SkPoint fFinalOrigin;
+        SkRect fBounds;
+        SkRect fInkBounds;
+    };
+
+    // Returns glyph diagnostics from the final laid-out text blob cache.
+    virtual std::vector<GlyphDiagnostic> getGlyphDiagnostics() = 0;
+
     /* Returns path for a given line
      *
      * @param lineNumber  a line number
diff --git a/modules/skparagraph/src/ParagraphImpl.cpp b/modules/skparagraph/src/ParagraphImpl.cpp
index 9ea8267545..831e622fa4 100644
--- a/modules/skparagraph/src/ParagraphImpl.cpp
+++ b/modules/skparagraph/src/ParagraphImpl.cpp
@@ -1239,6 +1239,51 @@ void ParagraphImpl::visit(const Visitor& visitor) {
     }
 }
 
+std::vector<Paragraph::GlyphDiagnostic> ParagraphImpl::getGlyphDiagnostics() {
+    std::vector<Paragraph::GlyphDiagnostic> result;
+    int lineNumber = 0;
+    for (auto& line : fLines) {
+        line.ensureTextBlobCachePopulated();
+        for (auto& rec : line.fTextBlobCache) {
+            if (rec.fBlob == nullptr || rec.fVisitor_Run == nullptr) {
+                continue;
+            }
+            const Run* sourceRun = rec.fVisitor_Run;
+            size_t sourceIndex = rec.fVisitor_Pos;
+            SkTextBlob::Iter iter(*rec.fBlob);
+            SkTextBlob::Iter::ExperimentalRun blobRun;
+            while (iter.experimentalNext(&blobRun)) {
+                for (int i = 0; i < blobRun.count; ++i, ++sourceIndex) {
+                    const SkPoint shapePosition = sourceRun->fPositions[sourceIndex];
+                    const SkPoint offset = sourceRun->fOffsets[sourceIndex];
+                    const SkVector advance =
+                            sourceRun->fPositions[sourceIndex + 1] - shapePosition;
+                    const SkPoint finalOrigin = blobRun.positions[i] + rec.fOffset;
+                    const SkRect bounds = blobRun.font.getBounds(blobRun.glyphs[i], nullptr);
+                    SkFontMetrics fontMetrics;
+                    blobRun.font.getMetrics(&fontMetrics);
+                    result.push_back({
+                            lineNumber,
+                            sourceRun->fIndex,
+                            blobRun.font,
+                            fontMetrics,
+                            blobRun.glyphs[i],
+                            SkToU32(sourceRun->globalClusterIndex(sourceIndex)),
+                            shapePosition,
+                            offset,
+                            advance,
+                            finalOrigin,
+                            bounds,
+                            bounds.makeOffset(finalOrigin),
+                    });
+                }
+            }
+        }
+        lineNumber += 1;
+    }
+    return result;
+}
+
 int ParagraphImpl::getLineNumberAt(TextIndex codeUnitIndex) const {
     if (codeUnitIndex >= fText.size()) {
         return -1;
diff --git a/modules/skparagraph/src/ParagraphImpl.h b/modules/skparagraph/src/ParagraphImpl.h
index 0fb0ba9a8e..c63c6799bf 100644
--- a/modules/skparagraph/src/ParagraphImpl.h
+++ b/modules/skparagraph/src/ParagraphImpl.h
@@ -210,6 +210,7 @@ public:
 
     void visit(const Visitor&) override;
     void extendedVisit(const ExtendedVisitor&) override;
+    std::vector<GlyphDiagnostic> getGlyphDiagnostics() override;
     int getPath(int lineNumber, SkPath* dest) override;
     bool containsColorFontOrBitmap(SkTextBlob* textBlob) override;
     bool containsEmoji(SkTextBlob* textBlob) override;
diff --git a/modules/skparagraph/src/Run.h b/modules/skparagraph/src/Run.h
index 2dc5bae737..bfe849e677 100644
--- a/modules/skparagraph/src/Run.h
+++ b/modules/skparagraph/src/Run.h
@@ -390,6 +390,7 @@ public:
         fRawAscent = a;
         fRawDescent = d;
         fRawLeading = l;
+        fDiagnosticRawLeading = l;
         fHeightInputAscent = a;
         fHeightInputDescent = d;
         fHeightInputLeading = l;
@@ -403,6 +404,7 @@ public:
         fRawAscent = ra;
         fRawDescent = rd;
         fRawLeading = rl;
+        fDiagnosticRawLeading = rl;
         fHeightInputAscent = ra;
         fHeightInputDescent = rd;
         fHeightInputLeading = rl;
@@ -418,6 +420,7 @@ public:
         fRawAscent = metrics.fAscent;
         fRawDescent = metrics.fDescent;
         fRawLeading = metrics.fLeading;
+        fDiagnosticRawLeading = metrics.fLeading;
         fHeightInputAscent = metrics.fAscent;
         fHeightInputDescent = metrics.fDescent;
         fHeightInputLeading = metrics.fLeading;
@@ -435,6 +438,7 @@ public:
         fRawAscent = std::min(fRawAscent, run->ascent());
         fRawDescent = std::max(fRawDescent, run->descent());
         fRawLeading = std::max(fRawLeading, run->leading());
+        fDiagnosticRawLeading = std::max(fDiagnosticRawLeading, run->leading());
         fHeightInputAscent = std::min(fHeightInputAscent, run->ascent());
         fHeightInputDescent = std::max(fHeightInputDescent, run->descent());
         fHeightInputLeading = std::max(fHeightInputLeading, run->leading());
@@ -447,6 +451,7 @@ public:
         fRawAscent = std::min(fRawAscent, other.fRawAscent);
         fRawDescent = std::max(fRawDescent, other.fRawDescent);
         fRawLeading = std::max(fRawLeading, other.fRawLeading);
+        fDiagnosticRawLeading = std::max(fDiagnosticRawLeading, other.fDiagnosticRawLeading);
         fHeightInputAscent = std::min(fHeightInputAscent, other.fHeightInputAscent);
         fHeightInputDescent = std::max(fHeightInputDescent, other.fHeightInputDescent);
         fHeightInputLeading = std::max(fHeightInputLeading, other.fHeightInputLeading);
@@ -459,6 +464,7 @@ public:
         fRawAscent = SK_ScalarMax;
         fRawDescent = SK_ScalarMin;
         fRawLeading = 0;
+        fDiagnosticRawLeading = SK_ScalarMin;
         fHeightInputAscent = SK_ScalarMax;
         fHeightInputDescent = SK_ScalarMin;
         fHeightInputLeading = SK_ScalarMin;
@@ -504,6 +510,7 @@ public:
             metrics.fRawAscent = fRawAscent;
             metrics.fRawDescent = fRawDescent;
             metrics.fRawLeading = fRawLeading;
+            metrics.fDiagnosticRawLeading = fDiagnosticRawLeading;
             metrics.fHeightInputAscent = fHeightInputAscent;
             metrics.fHeightInputDescent = fHeightInputDescent;
             metrics.fHeightInputLeading = fHeightInputLeading;
@@ -591,6 +598,9 @@ private:
     SkScalar fRawAscent;
     SkScalar fRawDescent;
     SkScalar fRawLeading;
+    // Preserves negative resolved-font leading for diagnostics without
+    // changing SkParagraph's existing line-layout behavior.
+    SkScalar fDiagnosticRawLeading;
     SkScalar fHeightInputAscent;
     SkScalar fHeightInputDescent;
     SkScalar fHeightInputLeading;
diff --git a/modules/skparagraph/src/TextLine.cpp b/modules/skparagraph/src/TextLine.cpp
index 4aaae6816a..55ea972b2c 100644
--- a/modules/skparagraph/src/TextLine.cpp
+++ b/modules/skparagraph/src/TextLine.cpp
@@ -1146,6 +1146,27 @@ LineMetrics TextLine::getMetrics() const {
             ? this->baseline()
             : this->height() - this->sizes().descent());
     result.fLineNumber = this - fOwner->lines().data();
+    result.fRawAscent = -this->sizes().rawAscent();
+    result.fRawDescent = this->sizes().rawDescent();
+    result.fRawLeading = this->sizes().rawLeading();
+    result.fEffectiveAscent = InternalLineMetrics::useQtLikeIntegerMetrics()
+        ? this->sizes().layoutAscent()
+        : -this->sizes().ascent();
+    result.fEffectiveDescent = InternalLineMetrics::useQtLikeIntegerMetrics()
+        ? this->sizes().layoutDescent()
+        : this->sizes().descent();
+    result.fEffectiveLeading = InternalLineMetrics::useQtLikeIntegerMetrics()
+        ? this->sizes().layoutLeading()
+        : this->sizes().leading();
+    result.fHeightInputAscent = this->sizes().heightInputAscent();
+    result.fHeightInputDescent = this->sizes().heightInputDescent();
+    result.fHeightInputLeading = this->sizes().heightInputLeading();
+    result.fHeightInputRawLeading = this->sizes().heightInputRawLeading();
+    result.fLineHeightBranch = this->sizes().lineHeightBranch();
+    result.fNextLineBaselinePitch = this->height();
+    result.fLineBoxHeight = InternalLineMetrics::useQtLikeIntegerMetrics()
+        ? this->sizes().lineBoxHeight()
+        : this->height();
 
     // Fill out the style parts
     this->iterateThroughVisualRuns(false,
