diff --git a/src/ports/SkFontHost_FreeType.cpp b/src/ports/SkFontHost_FreeType.cpp
index 84afa4b32e..ba3982d507 100644
--- a/src/ports/SkFontHost_FreeType.cpp
+++ b/src/ports/SkFontHost_FreeType.cpp
@@ -955,6 +955,15 @@ bool SkTypeface_FreeType::onGetQtLikeGlyphAdvanceDiagnostics(
     }
     *diagnostics = {};
 
+    const QtLikeAdvanceKey cacheKey{glyph, SkScalarToFDot6(textSize)};
+    {
+        SkAutoMutexExclusive lock(fQtLikeAdvanceCacheMutex);
+        if (const auto* cached = fQtLikeAdvanceCache.find(cacheKey)) {
+            *diagnostics = *cached;
+            return true;
+        }
+    }
+
     AutoFTAccess fta(this);
     FT_Face face = fta.face();
     if (!face || !FT_IS_SCALABLE(face)) {
@@ -1024,9 +1033,13 @@ bool SkTypeface_FreeType::onGetQtLikeGlyphAdvanceDiagnostics(
     diagnostics->fFaceFlags = static_cast<int>(face->face_flags);
     diagnostics->fStyleFlags = static_cast<int>(face->style_flags);
     diagnostics->fNumGlyphs = static_cast<int>(face->num_glyphs);
 
     FT_Activate_Size(oldSize);
     FT_Set_Transform(face, &oldTransform, &oldDelta);
+    {
+        SkAutoMutexExclusive lock(fQtLikeAdvanceCacheMutex);
+        fQtLikeAdvanceCache.set(cacheKey, *diagnostics);
+    }
     return true;
 }
 
diff --git a/src/ports/SkTypeface_FreeType.h b/src/ports/SkTypeface_FreeType.h
index e34d655017..614de1a928 100644
--- a/src/ports/SkTypeface_FreeType.h
+++ b/src/ports/SkTypeface_FreeType.h
@@ -18,6 +18,7 @@
 #include "include/private/base/SkNoncopyable.h"
 #include "include/private/base/SkTArray.h"
 #include "src/base/SkSharedMutex.h"
+#include "src/core/SkTHash.h"
 #include "src/utils/SkCharToGlyphCache.h"
 
 class SkFontData;
@@ -86,12 +87,34 @@ protected:
     static void FontDataPaletteToDescriptorPalette(const SkFontData&, SkFontDescriptor*);
 
 private:
+    struct QtLikeAdvanceKey {
+        SkGlyphID fGlyph = 0;
+        int32_t fTextSize26Dot6 = 0;
+
+        bool operator==(const QtLikeAdvanceKey& other) const {
+            return fGlyph == other.fGlyph &&
+                   fTextSize26Dot6 == other.fTextSize26Dot6;
+        }
+
+        struct Hasher {
+            size_t operator()(const QtLikeAdvanceKey& key) const {
+                return (static_cast<size_t>(key.fGlyph) * 16777619u) ^
+                       static_cast<uint32_t>(key.fTextSize26Dot6);
+            }
+        };
+    };
+
     mutable SkOnce fFTFaceOnce;
     mutable std::unique_ptr<FaceRec> fFaceRec;
 
     mutable SkSharedMutex fC2GCacheMutex;
     mutable SkCharToGlyphCache fC2GCache;
 
+    mutable SkMutex fQtLikeAdvanceCacheMutex;
+    mutable skia_private::THashMap<QtLikeAdvanceKey,
+                                   QtLikeGlyphAdvanceDiagnostics,
+                                   QtLikeAdvanceKey::Hasher> fQtLikeAdvanceCache;
+
     mutable SkOnce fGlyphMasksMayNeedCurrentColorOnce;
     mutable bool fGlyphMasksMayNeedCurrentColor;
 
